const [a,b,c,d] = [3,5,1,15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a*c
const divisao = d / a
const modulo = d % 2

console.log(soma,subtracao,multiplicacao,divisao,modulo)
/* o nome dos operadores quando entre os operandos é infix,
quando antes de um operando é prefix
quando após o operando é postfix*/
